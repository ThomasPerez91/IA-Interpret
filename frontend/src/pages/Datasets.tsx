import styles from "./css/Datasets.module.css";

export const Datasets: React.FC = () => {
  return (
    <div className={styles.wrap}>
      <h2>Jeu de données</h2>
      <p>À venir : upload CSV, pré-traitements, aperçu & validation.</p>
    </div>
  );
};
